# wiki/ — index

This file is regenerated by `polyforge:doc-ingest` once Phase 2 ships. While Phase 0 scaffolding is the only active component, this index stays minimal.

## Patterns

_None yet._ Stable patterns synthesized from `wiki/lessons/_raw.jsonl` will appear here once ≥ 3 entries share a `key` (default threshold; configurable via `workspace.yaml > docs.graduation.lessons_min_tickets`).

## Glossary

See `wiki/glossary/` (one markdown file per term, populated as ingest detects new internal terminology).

## Source streams

- `wiki/specs/` — promoted from ticket-scoped `.specs/` at `/polyforge:commit` step M5 (Phase 1+, currently writes to `<doc-repo>/specs/` until Phase 1 path migration ships).
- `wiki/plans/` — same flow as specs.
- `wiki/retros/` — written by `/polyforge:retro` post-merge (Phase 1+, currently writes to `<doc-repo>/retros/`).

## Schema

See [`SKILL.md`](SKILL.md) for the contract that polyforge agents and humans MUST follow when writing into wiki/.
